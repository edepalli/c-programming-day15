/*print factors of a given number*/ 
#include<stdio.h>
int main()
{ 
    int n,i=1;
    printf("enter a number:");
    scanf("%d",&n);
    printf("factors are:");
    while(i<=n)
    { 
      if((n%i)==0)
      printf("\n%d",i);
      i++;
	}
	return 0;
}
